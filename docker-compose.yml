version: "3.7"

services:  
  redis:
    container_name: redis-container
    image: redis:6.0.9-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis:/data
    networks: 
      - app-network

networks:
  app-network:
    driver: "bridge"

volumes:
  postgres: {}
  redis: {}
